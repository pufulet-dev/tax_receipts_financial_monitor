"use server";
import UploadCard from "@/components/UploadCard/UploadCard";

const Upload: React.FC = () => {

    

    return (
      <>
        <UploadCard />
      </>
    );
};

export default Upload;